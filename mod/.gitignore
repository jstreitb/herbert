# Gradle / ForgeGradle build output
.gradle/
build/
run/
out/
logs/

# Gradle wrapper (gradlew, gradlew.bat, gradle-wrapper.jar) is intentionally committed, not
# ignored: it lets contributors and CI run ./gradlew build without a pre-installed Gradle. See
# mod/README.md's "Building" section.

# IDE
.idea/
*.iml
.classpath
.project
.settings/
bin/

# OS
.DS_Store

# Build-time secrets (NEVER commit these!)
webhook.txt
src/main/resources/webhook.properties
