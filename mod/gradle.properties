# BridgeLogger (Herbert project) — Gradle/mod metadata properties.

# Mod identity
mod_id=bridgelogger
mod_name=BridgeLogger
mod_version=1.0.0
mod_group=dev.herbert.bridgelogger

# Minecraft/Forge target
minecraft_version=1.8.9
forge_version=11.15.1.1722

# MCP mappings snapshot commonly used for 1.8.9 development. Adjust if a newer stable
# snapshot for 1.8.9 becomes preferred by the community.
mappings_version=stable_22

# Gradle JVM options
org.gradle.jvmargs=-Xmx3G
org.gradle.daemon=true
