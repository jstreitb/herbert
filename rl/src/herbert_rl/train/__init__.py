"""Training layer: custom PPO rollout collection (`rollout.py`), shared setup (`setup.py`), and
the three CLI entry points -- `train.py`, `smoketest.py`, `evaluate.py`.
"""

from __future__ import annotations
