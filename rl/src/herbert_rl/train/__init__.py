# SPDX-License-Identifier: MIT
"""Training layer: custom PPO rollout collection, shared setup, and the CLI entry points.

Custom PPO rollout collection lives in `rollout.py`, shared setup in `setup.py`, and the
three CLI entry points are `train.py`, `smoketest.py`, `evaluate.py`.
"""

from __future__ import annotations
